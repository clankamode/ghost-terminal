# Optional. Not required for local play or the localStorage leaderboard.
# Only used if you wire src/lib/runSave.ts (unused by the app UI today).
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
